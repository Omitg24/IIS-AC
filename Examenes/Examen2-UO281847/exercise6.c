#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
	int  var = 5;
	int *res = NULL;
	point = (int *) malloc( sizeof(int) );
	free( res ); 
	point = (int *) 4;
	printf("P: %d\n", *res);
}

