#include <stdio.h>
//TODO; Add the required #include directive
#include <math.h>

int main()
{
	double num = 9.0;;
	double root = sqrt(num);
	printf("The square root of %f is %f\n", num, root);
	return 0;
}
